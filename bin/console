#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'es_tractor'

begin
  require 'pry'
  Pry.start
rescue LoadError
  require 'irb'
  IRB.start
end
